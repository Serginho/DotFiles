#Functions 
jdk() {
        version=$1
        export JAVA_HOME=$(/usr/libexec/java_home -v "$version");
        java -version
 }

function compress(){
	directory=$1
	tar c $directory | pv | pixz -p 12 -0 > $directory.tar.xz 
}
